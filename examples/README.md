# Database Schema Example

This example shows a possible SQL structure for storing hierarchical data
used by Hierarchical Structure Builder.

It is not required by the library and can be adapted to your own backend.

You can use string identifiers, not use sort and ordinal fields, use other primary keys, etc.